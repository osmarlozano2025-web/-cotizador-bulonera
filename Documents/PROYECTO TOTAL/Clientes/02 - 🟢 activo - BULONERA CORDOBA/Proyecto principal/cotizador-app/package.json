{
  "name": "cotizador-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently -n api,web -c magenta,cyan \"npm run dev:api\" \"npm run dev:client\"",
    "dev:api": "node server/dev-api.mjs",
    "dev:client": "cd client && npm run dev",
    "dev:server-legacy": "cd server && npm run dev",
    "migrate": "node server/dev-php.mjs api-php/migrate.php",
    "test:precios": "node server/dev-php.mjs api-php/tests/precios-test.php",
    "build": "cd client && npm install && npm run build",
    "start": "NODE_ENV=production node server/index.js",
    "install:all": "cd server && npm install && cd ../client && npm install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
